# Copy to .env.production before `npm run build` (file is gitignored).
# API host for fetch/auth routes (login/logout/callback). Logout still returns to https://drovashop.com via FRONTEND_URL on the API.
VITE_API_URL=https://app.drovashop.com
